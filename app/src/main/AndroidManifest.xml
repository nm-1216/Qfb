<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.sy.qfb">

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".ble.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.MainActivity"/>
        <activity android:name=".ui.MeasureSubjectActivity"/>
        <activity android:name=".ui.MeasureActivity"/>

        <activity android:name=".ui.DownloadActivity"/>
        <activity android:name=".ui.UploadActivity"/>

        <activity android:name=".ble.activity.AchartAcitivty"/>
        <activity android:name=".ble.activity.DeviceControlActivity"/>
        <activity android:name=".ble.activity.DeviceScanActivity"/>
        <activity android:name=".ble.activity.SdcardShow"/>

        <service android:name=".ble.service.BluetoothLeService" android:enabled="true"/>
    </application>

</manifest>
